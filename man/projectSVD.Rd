% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projectSVD.R
\name{projectSVD}
\alias{projectSVD}
\title{Right project matrix}
\usage{
projectSVD(mat, u, d, nDim, v = NULL)
}
\arguments{
\item{mat}{A numeric or sparse matrix with features as rows and observations
(e.g., cells) as columns.}

\item{u}{A numeric matrix containing the left singular vectors of the SVD.}

\item{d}{A numeric vector containing the singular values corresponding to
the columns of \code{u}.}

\item{nDim}{Integer specifying the number of SVD dimensions to return.}

\item{v}{Optional numeric matrix containing the right singular vectors.
If \code{NULL}, \code{v} is calculated from \code{mat}, \code{u}, and \code{d}. Default is \code{NULL}.}
}
\value{
A numeric matrix with observations as rows and SVD dimensions as
columns. Row names are taken from \code{colnames(mat)}, and columns are named
\code{"LSI1"}, \code{"LSI2"}, ..., \code{"LSIn"}.
}
\description{
Projects observations from a matrix into a lower-dimensional singular value
decomposition (SVD) space using supplied left singular vectors and singular
values. If the right singular vectors are not supplied, they are calculated
from the input matrix.
}
\details{
When \code{v} is not supplied, it is calculated as
\deqn{V = X^T U D^{-1},}
where \eqn{X} is \code{mat}, \eqn{U} is \code{u}, and \eqn{D} is the diagonal matrix
containing the singular values \code{d}.

The projected coordinates are then obtained from \eqn{VD}.
}
